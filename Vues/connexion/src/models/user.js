export default class User {
    constructor(name, email, address, password, roleId) {
        this.name = name;
        this.email = email;
        this.address = address;
        this.password = password;
        this.roleId = roleId;
    }
}